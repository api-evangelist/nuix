{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/api-evangelist/nuix/refs/heads/main/json-schema/nuix-rest-microsoft365versionfilteroptions.json",
  "title": "Microsoft365VersionFilterOptions",
  "description": "The version filter options.",
  "type": "object",
  "properties": {
    "versionRetrievalEnabled": {
      "type": "boolean",
      "description": "True if all versions should be retrieved, false for current version only.",
      "default": false
    },
    "versionRetrievalLimit": {
      "type": "integer",
      "description": "Integer value limiting the number of versions retrieved if versionRetrievalEnabled is set to true. -1 (all available versions)"
    }
  }
}